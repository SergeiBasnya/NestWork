NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_WS_URL=ws://localhost:4000
NEXT_PUBLIC_PILOT_EMAIL=contact@nestwork.site

# The public-safe default is "original". Only a private deployment that owns
# and installs the optional commercial library may set this to "full".
NEXT_PUBLIC_ASSET_LIBRARY=original
